---
- hosts: all
  become: yes
  tasks:
    - name: Print Hello message
      debug:
        msg: Hello, Geek World!

    - name: Add the user
      ansible.builtin.user:
        name: gidra
        comment: Alex Gidra
    
    - name: Update cache install apps
      apt:
        name: "{{ item }}"
        update_cache: yes
      loop:
        - curl
        - net-tools
        - nmap
        - mtr
        - socat
